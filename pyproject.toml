[project]
name = "vander"
version = "0.2.0"
description = "Graph a command's number output live in your terminal"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "urwid==4.0.0",
]
